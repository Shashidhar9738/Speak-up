# Secrets — never commit. Use .env.example as the template.
.env
.env.local
.env.*.local

node_modules/
dist/

# Runtime data — real complaints, employee email addresses, and appreciation
# records that name individuals. None of it belongs in version control.
backend/data/
!backend/data/.gitkeep

npm-debug.log*
*.log
.DS_Store
Thumbs.db
